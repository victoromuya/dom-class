// javascript modules help to break codes into seperate files


const message = () => {
    const name = "Ayo"
    const age = 40;

    console.log(name + " is " + age + " years old ")
    
    return name + " is " + age + " years old ";
    
};

export default message